import { UsageChartDirective } from './usage-chart.directive';

describe('UsageChartDirective', () => {
  it('should create an instance', () => {
    const directive = new UsageChartDirective();
    expect(directive).toBeTruthy();
  });
});
